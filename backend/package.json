{
  "name": "backend",
  "module": "server.js",
  "type": "module",
  "scripts": {
    "build:frontend": "cd ../frontend && bun install && bun run build",
    "start": "bun server.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5"
  }
}