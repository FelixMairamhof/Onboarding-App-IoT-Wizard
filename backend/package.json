{
  "name": "backend",
  "module": "server.js",
  "type": "module",
  "scripts": {
    "build:frontend": "cd ../frontend && bun install && bun run build",
    "start": "bun install && bun run build:frontend && bun server.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "path": "^0.12.7",
    "pg": "^8.12.0"
  }
}